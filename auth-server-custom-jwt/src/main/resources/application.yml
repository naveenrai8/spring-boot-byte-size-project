spring:
  application:
    name: authentication-service
  h2:
    console:
      path: /h2-console
      enabled: true
  datasource:
    username: admin
    password: admin
    url: jdbc:h2:mem:userdb
    driver-class-name: org.h2.Driver
jwt:
  # create JWT secret using https://jwtsecrets.com/#generator and must be atleast 192 bits
  secret: 
server:
  port: 8080
